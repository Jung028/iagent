{
  "version": "0.2.0",
  "configurations": [
    
    {
      "name": "Attach to iAgent",
      "type": "debugpy",
      "request": "attach",
      "connect": {
        "host": "localhost",
        "port": 5678
      },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/services/iagent-center/src",
          "remoteRoot": "${workspaceFolder}/services/iagent-center/src"
        }
      ],
      "justMyCode": false
    }
  ]
}
